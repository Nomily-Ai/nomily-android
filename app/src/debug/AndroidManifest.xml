<?xml version="1.0" encoding="utf-8"?>
<!--
  Only active in the debug variant. AdvDumpActivity is a real-device forensics probe.
  This class does not exist in the release build, nor does this declaration.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <activity
            android:name=".AdvDumpActivity"
            android:exported="true"
            android:label="Adv Dump (debug)" />
        <activity
            android:name=".CaptureClipActivity"
            android:exported="true"
            android:label="Capture Clip (debug)" />
        <activity
            android:name=".DeviceWipeActivity"
            android:exported="true"
            android:label="Device Wipe (debug)" />
    </application>

</manifest>
